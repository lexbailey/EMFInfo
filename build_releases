#!/usr/bin/env xonsh

$RAISE_SUBPROC_ERROR = True

$PATH.insert(0,"$PWD/sdcc-4.4.0/bin/")

mkdir -p releases
for target in [
        "zxspec48.tap",
        "linux",
        "msdos.exe",
        "msdos_textonly.exe",
    ]:
    $name = f"emfinfo_{target}"
    make $name
    cp $name releases/$name
